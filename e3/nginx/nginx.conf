http {
   upstream backend {
       server backend1:8080;
       server backend2:8082;
   }

   server {
       listen 8888;

       location / {
           proxy_pass http://backend;
       }
   }
}

events { }